'use strict';jest.dontMock('../app/counter.jsx');

describe('counter', function () {
  it('gets passed 1 and INCREMENT and return 2 ', function () {
    var counter = require('../redux/counter.jsx');
    expect(counter(1, 'INCREMENT')).toBe(2);});});